import 'package:flutter/material.dart';

class ItemListUnloaded extends StatelessWidget {
  const ItemListUnloaded({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pas de donnée pour le moment"),
    );
  }
}
