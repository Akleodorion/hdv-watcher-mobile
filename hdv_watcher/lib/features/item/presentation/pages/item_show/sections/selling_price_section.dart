import 'package:flutter/material.dart';

class SellingPriceSection extends StatelessWidget {
  const SellingPriceSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 100,
    );
  }
}
